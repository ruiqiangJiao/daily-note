# 执行命令

```
git checkout master
mkdocs serve
mkdocs build
git add docs/program/
git commit -m 'add '
git push origin master
git checkout gh-pages
rm docs/
mv site/ docs/
git add docs/
git commit -m 'add'
```

